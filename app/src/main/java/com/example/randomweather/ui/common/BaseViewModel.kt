package com.example.randomweather.ui.common

import androidx.lifecycle.ViewModel
import kotlinx.coroutines.CoroutineDispatcher

open class BaseViewModel(uiDispatcher: CoroutineDispatcher) : ViewModel()
